spring:
  application:
    name: productService

  datasource:
    url: jdbc:postgresql://localhost:5433/lakepop_db
    username: postgres
    password: 127001
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8086
